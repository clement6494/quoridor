#include <stdio.h>
#include <stdlib.h>

void bouge(int tab[17][17] ,t_joueur tour_de );

void bouge(int tab[17][17], t_joueur tour_de ){
int x;
char y;
int distance;

// selection case prochain coup
clear_entete_bas();
printf("selectionnez une colonne");
do {scanf("%d",x);
}
while ((x<=1)||(x>9));


printf("selectionnez une colonne");

do {scanf("%c",y);
}
while ((y<A)||((y>I)&&(y<a))||(y>i));
if ((y<a))||(y>i)){
y-=32;}

distance=abs(x- tour_de.lastx)+abs(y- tour_de.lasty);
if (distance>2){
    printf("TROP LOIN !!!");
    bouge(int tab[17][17], t_joueur tour_de );}

if (distance==2){
    if ((joueur_entre(nombre_de_joueurs)==1)&&(barrière_bloque(tabn[17][17])==0)&&(joueur_sur_case()==0)){
       tabn_l[17][17]=tabn[17][17];
       tabn[x][y-16]=tabn[lastx][lasty];
        tabn[lastx][lasty]=0;
        affichage();
    else {
        printf("mouvement impossible");
        bouge(int tab[17][17], t_joueur tour_de )
    }
    }

}

}
